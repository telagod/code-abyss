# mythos-kernel-merge — drafted implementation specs (P2–P5)

> Auto-extracted from workflow wf_40b9d7e0-39e journal. These are DRAFTS to
> review+apply by hand on branch feat/mythos-kernel-merge. Edits' oldString
> anchors must be re-confirmed against current files before applying.


## P0 — vendoring mechanism + kernel-directory foundation + router fusion wiring

Load-bearing path convention (all downstream work depends on it): the mythos kernel is vendored as committed real files at `skills/_kernel/<bundle>/` (nine bundles: doctrine, methods, character, loop-engineering, frontend, backend, hardware, ml, security), each a SKILL.md-bearing dir sitting alongside — never colliding with — the 30 gerund exec skills in `skills/`. Vendoring is a build-time sync script (`scripts/sync-mythos.js`), NOT a git submodule, because code-abyss ships as an npm tarball and `npm pack` does not populate submodules (a submodule kernel would ship EMPTY to every user); the kernel must be in-tree, and only a script can apply the required frontmatter transform. Distribution needs ZERO manifest edits: `packs/abyss/manifest.json`'s existing `src:"skills"` entry globs all children (only `SKIP=[__pycache__,.pyc,...]` is filtered — `_`-prefixed dirs like the existing `_lib` are copied), so `_kernel/` lands in `~/.{claude,codex,gemini,openclaw}/skills/` on all 4 hosts for free, and `collectSkills` recursion auto-generates `/doctrine` … `/security` slash-commands (Claude .md, Gemini .toml). VERIFIED: sync ran clean (exit 0), all 9 bundles register as invocable without the registry throwing (14 total invocable = 5 pre-existing + 9 kernel), and command content generates with skillPath `~/.claude/skills/_kernel/doctrine/SKILL.md`.</summary>
<parameter name="verdict">distribute — vendor mythos as committed real files under skills/_kernel/ so the existing skill-glob copy + command-gen carry all 9 bundles to 4 hosts with no manifest/adapter change; layer a kernel-router shared fragment above exec skill-routing.

**files:**
- `/home/telagod/project/code-abyss/scripts/sync-mythos.js` (create) — Build-time one-way vendor of mythos's 9 bundles into skills/_kernel/. Chosen over git-submodule because npm pack ships no submodule contents AND the copy must inject `user-invocable: true` into each SKILL.md (skill-registry.js:84 hard-requires it or command-gen throws) — a transform impossible on a submodule's checked-out files. English rule text preserved byte-for-byte; writes .sync-meta.json provenance (mythos git commit) for single-source audit; idempotent. VERIFIED runs exit 0, patches 9 SKILL.md.
- `/home/telagod/project/code-abyss/config/personas/_shared/kernel-router.md` (create) — Router-fusion layer: the mythos router re-expressed as a code-abyss shared-behavior fragment, host-agnostic relative paths (`skills/_kernel/<bundle>/SKILL.md`) matching skill-routing.md's convention. Governs the 9 kernel bundles (L-KERNEL discipline + L-JUDGE domain) and states it sits ABOVE exec routing / wins over voice. Ships automatically — package.json `files` already includes `config/personas/_shared/`. Full body created.
- `/home/telagod/project/code-abyss/bin/lib/style-registry.js` (edit) — Assemble kernel-router.md into the always-on runtime guidance ABOVE exec skill-routing.md (layered router decision). loadSharedBehavior() reads SHARED_FILES_ORDER in sequence and renderRuntimeGuidance() bakes the result into every host's context (Claude/Gemini/Codex AGENTS/GEMINI.md + OpenClaw AGENTS.md/SOUL.md), so this one insert distributes the kernel router to all 4 hosts including the no-router OpenClaw tier.
- `/home/telagod/project/code-abyss/package.json` (edit) — Expose the vendor step as an npm script so it is discoverable and can later be wired to a prepack hook (dev-time refresh of skills/_kernel/ before publish).

**routing:**
- skills/_kernel/frontend → applying-ui-design-system, developing-mobile-apps  L-JUDGE web/UI taste routes down to L-EXEC how-to; compose wired in P2-P4
- skills/_kernel/backend → designing-architectures, provisioning-infrastructure, engineering-data-pipelines, automating-devops  stack/logic/data/production judgment -> concrete exec skills
- skills/_kernel/hardware → designing-hardware-products, operating-kicad-eda  part selection / bench-debug judgment -> EDA + product exec
- skills/_kernel/ml → building-agent-systems  agent pipeline judgment composes onto existing agent exec skill; classic-ML/eval half is net-new import (no exec skill exists)
- skills/_kernel/security → securing-systems, analyzing-security, architecting-security, defending-applications, detecting-and-responding, securing-cloud-and-supply-chain  threat-model + authorization gate judgment -> 6 security exec skills; kernel security gate runs FIRST on offensive requests

**conflicts:**
- HARD (resolved by the sync script): mythos SKILL.md ships only `name`+`description`; code-abyss skill-registry.js:84 THROWS `缺少必填 frontmatter 字段 'user-invocable'` on any SKILL.md lacking it, which would crash command generation and abort install. Resolution: sync-mythos.js injects `user-invocable: true` into each bundle's frontmatter at vendor time (idempotent). Verified all 9 now register without throwing.
- STALE (flagged, fix deferred to P5 per design §7): config/personas/_shared/skill-routing.md:6-9 routes to non-existent skills `building-ai-systems` and `coordinating-agents`. The new kernel-router.md layered above it does not remove these; they remain dangling. When routing moves to kernel-driven (P5) skill-routing.md must be corrected to real exec-skill names (e.g. building-agent-systems).
- LOW: character/ bundle carries hooks/check_banned_openers.py — it is copied verbatim into skills/_kernel/character/hooks/ but NOT wired to any Stop hook here (no Stop hook exists in code-abyss today). That wiring is P2/P3 scope; P0 only vendors the file so it is present when the injectors are extended.
- NONE on manifest: no change to packs/abyss/manifest.json is needed or made — adding an explicit _kernel entry would be redundant with the existing skills glob and risks double-install; deliberately omitted (anti-gold-plating).

**risks:**
- Command-name surface grows by 9: /doctrine /methods /character /loop-engineering /frontend /backend /hardware /ml /security now generate on Claude and Gemini. Verified none collide with the 30 gerund exec skills, but they DO shadow the always-on kernel-router pointers — intended (lazy-load per design T1), but worth a release note.
- The vendored skills/_kernel/ is now a committed artifact that can drift from mythos if someone edits it in place. Mitigated by .sync-meta.json provenance + the header comment 'do not hand-edit', but nothing enforces re-sync; a prepack hook (deferred) would close it.
- OpenClaw gets skills/_kernel/ copied to ~/.openclaw/skills/ via the manifest even though it has no skill/router runtime — the bundles are read-on-demand files only. Consistent with design §3 (rules 留盘, model reads on demand) but means ~9 bundle dirs of English rules sit unused unless the AGENTS.md/SOUL.md kernel-router text prompts a Read.
- sync-mythos.js uses fs.cpSync with a filter (Node >=16.7); package.json engines is >=18 so safe, but the filter is basename-based — a nested dir named e.g. 'archive' inside a bundle would be excluded only if added to EXCLUDE. Current 9 bundles have no such dirs (verified).

## mythos bundle "hardware" → code-abyss (COMPOSE: kernel judgment routes into exec skills designing-hardware-products + operating-kicad-eda)

The hardware bundle is L-JUDGE (electrical/part/firmware/bring-up judgment) and does not REPLACE any shallow config counterpart — config/personas/_shared/ has NO hardware engine (grep found only the kernel-router.md line at :26). So this is a pure COMPOSE: vendor the 6 mythos hardware files verbatim under skills/_kernel/hardware/ (already done by P0 scaffold — confirmed byte-identical to mythos except the injected `user-invocable: true` frontmatter), then wire bidirectional routing between the kernel judgment bundle and the two exec skills. Down-link (judgment→exec) lives in code-abyss-owned kernel-router.md and skill-routing.md — NOT in the vendored kernel body, because sync-mythos.js does `fs.rmSync(KERNEL_DIR)` + verbatim `cpSync` (scripts/sync-mythos.js:92-99) and only transforms frontmatter, so any body edit to skills/_kernel/hardware/SKILL.md is clobbered on re-sync, and editing mythos upstream would pollute the portable bundle with code-abyss-specific exec paths. Up-link (exec→judgment) lives in the two exec SKILL.md "Companion skills" sections (code-abyss-owned, durable). P1 precedence preserved: every new link states the kernel outranks the exec skill's defaults on electrical/part decisions.</parameter>
<parameter name="verdict">compose — vendor hardware as kernel judgment (already in-tree at skills/_kernel/hardware/), then bidirectionally wire it to exec skills designing-hardware-products + operating-kicad-eda; no REPLACE because no shallow hardware engine exists in _shared/.

**files:**
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/SKILL.md` (create) — VENDORED (already present via P0 sync; do NOT hand-edit — clobbered by scripts/sync-mythos.js re-sync). Kernel judgment router. Byte-identical to mythos/hardware/SKILL.md plus injected `user-invocable: true`. Listed for completeness; no change in this slice.
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/parts.md` (create) — VENDORED verbatim (P0, present). Part/MCU selection judgment. No change.
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/circuits.md` (create) — VENDORED verbatim (P0, present). Power-tree/derating/battery-budget judgment. No change.
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/firmware.md` (create) — VENDORED verbatim (P0, present). Firmware-for-unattended-devices rules. No change.
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/bringup.md` (create) — VENDORED verbatim (P0, present). Bench debugging + first-power-up discipline. No change.
- `/home/telagod/project/code-abyss/skills/_kernel/hardware/traps.md` (create) — VENDORED verbatim (P0, present). Hardware trap/health catalog. No change.
- `/home/telagod/project/code-abyss/skills/designing-hardware-products/SKILL.md` (edit) — UP-LINK (exec→judgment): make the pipeline defer to kernel hardware judgment as first companion; assert P1 precedence.
- `/home/telagod/project/code-abyss/skills/operating-kicad-eda/SKILL.md` (edit) — UP-LINK (exec→judgment): the KiCad tool-driver had no companion section and no path to the electrical judgment behind routing/clearance decisions; add one before References.
- `/home/telagod/project/code-abyss/config/personas/_shared/kernel-router.md` (edit) — DOWN-LINK (judgment→exec) in the code-abyss-owned kernel router: name the two hardware exec skills so the assembled guidance routes from judgment to concrete execution. Durable across sync (this file is not vendored).
- `/home/telagod/project/code-abyss/config/personas/_shared/skill-routing.md` (edit) — Fill the MISSING hardware route in the currently-loaded exec router (skill-routing.md IS in SHARED_FILES_ORDER at style-registry.js:26; kernel-router.md is NOT yet, so today hardware has zero reachable route). Adds a Chinese exec-layer line pointing at both hardware skills and noting judgment-first.

**routing:**
- skills/_kernel/hardware/SKILL.md (judgment: parts/circuits/firmware/bringup/traps) → skills/designing-hardware-products/SKILL.md  Full-product pipeline is the execution surface for hardware judgment. Wired in config/personas/_shared/kernel-router.md (down) and designing-hardware-products Companion skills (up). Kernel outranks pipeline defaults on part/electrical/firmware-safety calls (P1).
- skills/_kernel/hardware/SKILL.md (§circuits.md routing/clearance/layout judgment) → skills/operating-kicad-eda/SKILL.md  KiCad MCP tool driver executes the PCB step; kernel supplies routing/derating/decoupling rationale. Wired in kernel-router.md (down) and a new operating-kicad-eda Companion skills section (up). Kernel wins over convenient tool defaults on any electrical rule.

**conflicts:**
- MISSING route, not a duplicate: config/personas/_shared/skill-routing.md (the only exec router in SHARED_FILES_ORDER, style-registry.js:19-27) has NO hardware entry at all, so before this slice hardware was unreachable from assembled guidance. Resolution: add the hardware line to skill-routing.md (edit above); no duplicate-priority clash because the domain was simply absent.
- kernel-router.md is authored but NOT wired into assembly: style-registry.js:19-27 SHARED_FILES_ORDER omits 'kernel-router.md', so my down-link there does not render until the kernel-spine (doctrine/methods REPLACE) slice inserts it into SHARED_FILES_ORDER. Resolution: my skill-routing.md edit gives hardware a working route in the meantime; flag the SHARED_FILES_ORDER insert as a cross-slice dependency owned by the spine slice (do NOT duplicate that edit here to avoid a double-insert conflict).
- English kernel vs Chinese exec layer: kernel-router.md + vendored kernel are English (mythos is English-only, sync-mythos.js:16 preserves byte-for-byte); skill-routing.md is Chinese code-abyss voice. Resolution: keep the two layers separated by concern — judgment/precedence in English (kernel-router.md), gerund-skill dispatch in Chinese (skill-routing.md) — and have the Chinese line explicitly defer judgment to `skills/_kernel/hardware/` so the language boundary follows the layer boundary rather than splitting a single decision.
- Vendored-body edit trap: the natural place for a down-link is the kernel hardware SKILL.md 'Scope and neighbors' section, but scripts/sync-mythos.js:92-99 does fs.rmSync(KERNEL_DIR)+verbatim cpSync and only patches frontmatter, so any body edit is silently clobbered on re-sync (script header explicitly says 'Do not hand-edit'). Resolution: down-link lives only in code-abyss-owned config files (kernel-router.md, skill-routing.md); the up-link lives in the exec skills — neither is under skills/_kernel/, so both survive re-sync.
- Pre-existing companion overlap: designing-hardware-products already links operating-kicad-eda as its Phase-3 companion (SKILL.md:47). Adding the kernel as the FIRST companion could read as competing routing. Resolution: order + wording — kernel is 'READ FIRST … what to choose and why', kicad stays 'Phase 3 detail'; they are how-vs-what, not two hows, so no dispatch ambiguity.
- OUT-OF-SCOPE stale note (flag only): skill-routing.md still references non-existent skills building-ai-systems and coordinating-agents (lines for AI/RAG and 多Agent). Not touched here — belongs to a dedicated stale-fix slice; my edit only adds the hardware line and does not depend on those broken lines.

**risks:**
- If the kernel-spine slice never wires kernel-router.md into SHARED_FILES_ORDER, the English down-link (kernel-router.md hardware line) is dead weight in assembled output; the Chinese skill-routing.md line is the only load-bearing route until then. Verify by grepping rendered guidance after both slices land.
- openclaw host has no router and static AGENTS.md/SOUL.md only: the /designing-hardware-products and /doctrine slash-commands won't exist there, but the vendored files still ship to ~/.openclaw/skills/_kernel/hardware/ (manifest glob). The compose links degrade to plain file-path references the model can Read — acceptable for a low-tier host, but the up/down navigation is manual there.
- Exec-skill up-links use relative paths (../_kernel/hardware/SKILL.md); these resolve only when both the exec skill and the kernel land as siblings under the same skills/ root — true on all 4 hosts per manifest glob, but breaks if a host flattens the skills tree. Confirmed sibling layout holds today (skills/_kernel and skills/designing-hardware-products co-located).

## frontend bundle (COMPOSE): vendored mythos frontend judgment routed down into code-abyss exec skills applying-ui-design-system + developing-mobile-apps; reciprocal upward judgment-gate on those exec skills.

The mythos `frontend` bundle is already vendored verbatim to `skills/_kernel/frontend/` (SKILL.md + process/styles/taste/craft/garbage.md) by the P0 scaffold, and kernel-router.md:23 already routes web-UI to it. IMPORT is therefore done — the vendored bundle IS the "general web-frontend judgment" (there is no generic web-frontend exec skill in code-abyss; applying-ui-design-system only selects named design-systems, developing-mobile-apps only covers mobile platforms). The remaining work is COMPOSE: wire the kernel judgment DOWN into the two named exec skills, and reciprocally gate those exec skills UP to the kernel. CRITICAL CONSTRAINT discovered in sync-mythos.js:90-100: the sync does `fs.rmSync(KERNEL_DIR)` then verbatim `cpSync`, applying only the `user-invocable:true` transform (lines 59-68). Any hand-edit to the vendored `skills/_kernel/frontend/SKILL.md` is destroyed on the next `node scripts/sync-mythos.js`, AND putting code-abyss exec-skill paths into mythos upstream would pollute the host-agnostic source of truth. So the down-route cannot be a hand-edit and cannot live in mythos. Resolution: a code-abyss-owned per-bundle sidecar `scripts/exec-routing/frontend.md` that sync-mythos.js appends to the vendored SKILL.md after copy (one generic hook, so all 9 bundle subagents drop sidecars without contending on the script). The up-route is a hand-edited Chinese judgment-gate at the top of each exec skill (code-abyss-owned, stable, survives everything). Relative paths verified against the existing cross-ref applying-ui-design-system/SKILL.md:44 (`../designing-architectures/SKILL.md`): exec skills are one level under skills/, _kernel/frontend is two levels, so kernel→exec is `../../<exec>/SKILL.md` and exec→kernel is `../_kernel/frontend/SKILL.md`.</parameter>
<parameter name="verdict">compose — kernel frontend judgment (imported/vendored, already present) routes DOWN into exec skills applying-ui-design-system + developing-mobile-apps via a sync-appended sidecar footer; exec skills gate UP to the kernel via a hand-edited judgment-gate. No REPLACE of a _shared/*.md (frontend has no shallow persona counterpart; kernel-router.md already supersedes routing).

**files:**
- `scripts/exec-routing/frontend.md` (create) — Code-abyss-owned execution-routing footer for the frontend kernel bundle. sync-mythos.js appends it to the vendored skills/_kernel/frontend/SKILL.md on every sync, so the down-route into exec skills survives the rmSync+verbatim-copy while mythos upstream stays host-agnostic. English, to match the kernel voice.
- `scripts/sync-mythos.js` (edit) — Add the ROUTING_DIR const + appendExecRouting() and call it in the vendor loop, so each vendored SKILL.md gets its code-abyss down-route footer appended after the verbatim copy. Generic hook — every bundle subagent contributes its own scripts/exec-routing/<bundle>.md with no edit contention here.
- `scripts/sync-mythos.js` (edit) — Define appendExecRouting after ensureUserInvocable.
- `scripts/sync-mythos.js` (edit) — Call appendExecRouting in the vendor loop.
- `skills/applying-ui-design-system/SKILL.md` (edit) — Reciprocal UP-route: a Chinese judgment-gate pointing to the kernel frontend bundle, so an agent that enters this exec skill directly is bounced up to the taste/direction judgment first. Written in Chinese to match this skill's voice; targets the English kernel (no rule-content translation, per sync contract).
- `skills/developing-mobile-apps/SKILL.md` (edit) — Reciprocal UP-route for the mobile exec skill: UI/visual decisions defer up to the kernel frontend judgment; this skill retains native platform mechanics. Clarifies the split so the compose does not double-own taste.

**routing:**
- skills/_kernel/frontend/SKILL.md (mythos frontend judgment — after direction set in process.md/styles.md/taste.md) → applying-ui-design-system  DOWN-route for named design-system execution (glass/liquid-glass/neubrutalism/claymorphism tokens, components, dark-mode, a11y). Wired via appended sidecar scripts/exec-routing/frontend.md path ../../applying-ui-design-system/SKILL.md. Reciprocal UP-gate added at applying-ui-design-system/SKILL.md:6.
- skills/_kernel/frontend/SKILL.md (mythos frontend judgment — surface classified as mobile in process.md) → developing-mobile-apps  DOWN-route when target is a mobile app rather than a web page; kernel keeps taste veto, exec owns native platform mechanics. Wired via sidecar path ../../developing-mobile-apps/SKILL.md. Reciprocal UP-gate added at developing-mobile-apps/SKILL.md:6.

**conflicts:**
- Re-sync destroys hand-edits: sync-mythos.js:90-100 does fs.rmSync(KERNEL_DIR) then verbatim cpSync, so a down-route hand-edited into skills/_kernel/frontend/SKILL.md is gone on the next sync. RESOLVED: down-route lives in code-abyss-owned scripts/exec-routing/frontend.md, appended by a new appendExecRouting() hook that runs after copy — deterministically reproduced every sync.
- Mythos purity: mythos frontend/SKILL.md is host-agnostic and must not name code-abyss exec skills (they don't exist in mythos). RESOLVED: the code-abyss-specific down-route stays entirely on the code-abyss side (sidecar + sync hook); mythos stays the untouched English source of truth.
- English kernel vs Chinese exec layer: the vendored kernel + the appended footer are English; applying-ui-design-system and developing-mobile-apps are Chinese. RESOLVED: keep the seam bilingual — footer English (matches kernel voice), UP-gate written in Chinese (matches exec voice) but links to the English kernel. No rule content is translated, honoring sync-mythos.js's byte-for-byte English contract.
- P1 precedence / duplicate ownership of taste: applying-ui-design-system carries a11y '共享铁律' (contrast/focus/reduced-motion) that could be mistaken for taste rules and clash with garbage.md. RESOLVED by explicit precedence text in both footer and gate: kernel owns direction/taste with garbage.md veto; exec owns token VALUES + platform mechanics only. The a11y rules are mechanics, not taste — no overlap, no duplicate priority rule.
- 9-subagent write contention on sync-mythos.js: if every bundle appended its own footer as a hardcoded map in the script, all 9 subagents would edit the same file. RESOLVED by the sidecar-per-bundle design: the script gets ONE generic appendExecRouting hook; each bundle contributes only scripts/exec-routing/<bundle>.md.
- artifact-design double-routing: mythos frontend/SKILL.md:29 already defers investment calibration to the harness artifact-design skill, and code-abyss also ships frontend-design/artifact-design in its skill list. NOT a conflict — kernel scopes artifact-design as calibration-only and this bundle governs direction/craft once building. No change needed; flagged so the L-SKIN subagent doesn't re-route it.

**risks:**
- The two exec-skill UP-gates are hand-edits to files code-abyss owns (not vendored) — stable, but if a future refactor renames skills/_kernel/frontend the relative links ../_kernel/frontend/SKILL.md silently rot. Mitigate with npm run verify:skills link-check (referenced in sync-mythos.js:118).
- Sidecar mechanism is new: if scripts/exec-routing/frontend.md is missing at sync time, appendExecRouting silently returns false and the kernel SKILL.md ships with NO down-route (agent stays in judgment, never reaches exec tokens). Low impact (degrades to current behavior) but worth a verify:skills assertion that each bundle with exec neighbors has its sidecar.
- developing-mobile-apps is routed under the frontend (web-UI) kernel; mythos frontend scope (SKILL.md:27-30) is explicitly the web visual/aesthetic layer. Mobile native UX taste is only partially covered by web taste — acceptable (taste/garbage principles transfer), but the gate deliberately narrows the kernel's mobile claim to 'UI 观感/风格' and leaves platform UX to the exec skill.
- Not my slice but flagged for the owning subagent: config/personas/_shared/skill-routing.md still references non-existent skills (building-ai-systems, coordinating-agents). It is superseded by kernel-router.md for routing; the frontend compose does not touch it. Backend/ml subagent should deprecate or fix it so stale exec routing doesn't shadow the kernel.

## mythos "methods" -> code-abyss kernel (REPLACE of config/personas/_shared/execution-chains.md + big-picture.md)

methods is a spine bundle (HOW to think), not a domain-judgment bundle, so it does NOT route down to one exec skill; it is cross-cutting, reached via kernel-router.md's methods line. Already vendored on disk at skills/_kernel/methods/ (SKILL.md, investigate.md, design.md, execute.md, verify.md, communicate.md) by scripts/sync-mythos.js with the frontmatter transform applied (added user-invocable: true above mythos name/description, verified skills/_kernel/methods/SKILL.md:1-5). Distribution is free: packs/abyss/manifest.json src:skills glob copies _kernel/ to all 4 hosts and collectSkills auto-generates /methods. The only code I own is the REPLACE in bin/lib/style-registry.js. Content mapping: execution-chains.md 执行链 -> investigate.md §5/§6 + execute.md §1; its 验证链 -> investigate.md §2 evidence rank order + kernel non-negotiable #3 done-gate; big-picture.md 大局观 -> design.md §1/§2. The replacement pointer already lives in config/personas/_shared/kernel-router.md but that file is NOT yet in SHARED_FILES_ORDER (style-registry.js:19-28) so it is currently unassembled; my array edit removes the two dead entries and inserts kernel-router.md at top so the deep pointer replaces the deleted inline text. P1 preserved: shared layer (L0, kernel+iron-laws) is emitted before styleContent (L3) in renderRuntimeGuidance (:322-330), so kernel outranks skin.</parameter>
<parameter name="verdict">replace — methods deep engine supersedes execution-chains.md + big-picture.md; delete both, drop from SHARED_FILES_ORDER, insert kernel-router.md so the methods pointer replaces the deleted inline content.

**files:**
- `bin/lib/style-registry.js` (edit) — REPLACE core: drop big-picture.md + execution-chains.md from the assembled shared layer and insert kernel-router.md at top. loadSharedBehavior loops this array with an existsSync guard (:33-37), so removed entries stop assembling and the deleted files cannot break the load.
- `config/personas/_shared/execution-chains.md` (delete) — Shallow 执行链/验证链 stub; superseded by skills/_kernel/methods/investigate.md §2/§5/§6 and execute.md §1. Its 验证链 also duplicated kernel non-negotiable #3 done-gate. Git history is the restore path.
- `config/personas/_shared/big-picture.md` (delete) — Shallow 大局观 stub; thinking-move core superseded by skills/_kernel/methods/design.md §1-§2. NOTE its BPMN/UML/C4/ArchiMate notation content has no home in methods kernel — relocate to backend bundle / designing-architectures (see conflicts).
- `skills/_kernel/methods/SKILL.md` (create) — ALREADY VENDORED by scripts/sync-mythos.js (verified on disk, frontmatter transform applied). Listed for package completeness; do not re-create. Router: triggers + route-by-moment table to the 5 sibling files.
- `skills/_kernel/methods/investigate.md` (create) — ALREADY VENDORED. Survey-first, evidence rank order (runtime>disk>docs>hearsay), verified-vs-hearsay, reconcile-frame, anomaly protocol, differential diagnosis. Replaces execution-chains.md 验证链.
- `skills/_kernel/methods/design.md` (create) — ALREADY VENDORED. Mine-the-requirement, decompose-by-mechanism, one-source-of-truth, degradation-safe, second-order check, write-for-weakest-operator. Replaces big-picture.md 大局观.
- `skills/_kernel/methods/execute.md` (create) — ALREADY VENDORED. Value-order+crash-safety, verify-each-rung, reversibility ladder, batch-vs-serialize, context budget, campsite-labeled. Replaces execution-chains.md 执行链.
- `skills/_kernel/methods/verify.md` (create) — ALREADY VENDORED. Different-lenses, execute-the-journey, finding-needs-failure-scenario, reconcile-reviewer-findings, re-verify-after-fix, honest-negative. Reinforces the P1 done-gate.
- `skills/_kernel/methods/communicate.md` (create) — ALREADY VENDORED. Rule=trigger+action+examples, lead-with-outcome, write-for-weakest-reader, split-by-audience (English for models), state-what-you-cant-do, maintenance-surface.

**conflicts:**
- MODELING-NOTATION CONTENT LOSS: big-picture.md carries BPMN/UML/C4/ArchiMate/Event-Storming mappings the methods kernel deliberately lacks (methods is thinking-moves, not notation). Deleting drops this. Resolution: relocate that pointer to the backend judgment bundle (skills/_kernel/backend/) or designing-architectures exec skill — hand to the backend-bundle task; do not silently regress architecture-modeling guidance.
- SHARED_FILES_ORDER COLLISION with the doctrine/scaffold task: my edit inserts kernel-router.md at top; the doctrine task (which created kernel-router.md) likely also wires it. Duplicate entry is harmless (concatenated twice) but ugly. Resolution: exactly ONE task inserts kernel-router.md; if doctrine owns it, my newString keeps only the two REMOVALS and drops the kernel-router insertion. De-dupe at package assembly.
- DUPLICATE PRIORITY RULE: execution-chains.md 验证链 (源码>脚本>文档) duplicates investigate.md §2 and kernel non-negotiable #3 done-gate. Keeping the Chinese stub = a drifting third copy (design.md §3 violation). Resolution: delete; single home = investigate.md §2 + doctrine done-gate.
- ENGLISH-vs-CHINESE LAYER: deleted files are Chinese (执行链/大局观); methods kernel is English (communicate.md §4: model-facing rules in English). Removal is language-correct, but surviving iron-laws.md/proactive.md stay Chinese, so the assembled layer is mixed. Resolution: intended — iron-laws stays Chinese as the P1 forbidden-zone law, kernel-router (English) bridges into the English kernel; methods rule-text must NOT be back-translated into a Chinese _shared restatement.
- P1 PRECEDENCE / POSITIONAL RISK: renderRuntimeGuidance emits shared(L0 kernel) before styleContent(L3) — good — but a persona posthistory.md (L4 末段强指令) is emitted LAST (:329-330), positionally after the kernel. Resolution: already textually fenced by iron-laws forbidden-zone and kernel-router 'kernel wins on conflict'; no methods code change, but posthistory must never carry a correctness/verify override — enforce in persona-authoring lint.
- ADJACENT STALE: skill-routing.md (stays in the array after my edit) references non-existent exec skills building-ai-systems and coordinating-agents. Not mine to fix but I touch the same array — flag for the skill-routing task.

**risks:**
- If the kernel-router.md insertion is dropped during collision reconciliation while the two shallow files are still deleted, the assembled shared layer loses ALL methods guidance (silent regression). Guard: array must contain kernel-router.md before the two removals land — same commit.
- loadSharedBehavior guards each file with existsSync (:33-37), so deleting the .md files cannot throw even if the array edit is missed — but a stale array entry then fails silently. Verify post-edit by rendering and grepping output for the 'Discipline kernel' heading and confirming 执行链/大局观 no longer appear.
- openclaw (low tier: static AGENTS.md+SOUL.md, no router/slash-commands) only gets methods if its adapter renders renderRuntimeGuidance's shared block. Confirm the openclaw adapter still emits the shared layer so the kernel-router pointer reaches it as text; otherwise openclaw loses methods entirely.
- Vendored kernel drift: skills/_kernel/methods/* are copies of /home/telagod/project/mythos/methods/. scripts/sync-mythos.js must re-run before release or the shipped kernel goes stale — make sync a release-gate step.

## Host distribution: gemini (tier=mid — native skills + commands/*.toml router; no Stop hook)

On gemini, the 9 kernel bundles need ZERO gemini-specific registration code: they are already vendored as real files at `skills/_kernel/<bundle>/SKILL.md` (verified: all 9 present with frontmatter names doctrine…security). The existing manifest skill-glob copies them to `~/.gemini/skills/_kernel/`, and `installGeneratedGeminiCommands` (core-install.js:101) → `collectInvocableSkills` (skill-registry.js:139 recurses through `_kernel/*`) auto-emits `~/.gemini/commands/{doctrine…security}.toml`, each pointing `Read ~/.gemini/skills/_kernel/<bundle>/SKILL.md` (command-generation.js:100,127). So the gemini-specific work is exactly two things: (1) the Stop-hook degradation — gemini CLI has no Stop event, so the ported `check_banned_openers` backstop CANNOT run; per mythos 'no silent caps' I add an explicit install-time degradation log + a durable manifest.degradations entry, and the character rule survives prompt-only via GEMINI.md. (2) Router fusion — gemini's GEMINI.md is rendered by `renderGeminiContext`→`renderRuntimeGuidance`→`loadSharedBehavior` (style-registry.js:30,338), which concatenates `config/personas/_shared/*` in SHARED_FILES_ORDER; I add a new `kernel-routing.md` fragment ABOVE `skill-routing.md` so the mythos judgment spine (doctrine/methods/character/loop-eng + 5 domain judges) fires before exec-skill selection, and I fix the STALE skill-routing.md (building-ai-systems / coordinating-agents don't exist).</parameter>
<parameter name="verdict">distribute — gemini registers all 9 kernel bundles for free via the existing skill-glob + toml command generator; only the Stop-hook degradation log (gemini-owned) plus the shared kernel-routing fusion fragment (consumed by GEMINI.md) are new. No adapter or manifest edit.

**files:**
- `bin/lib/lifecycle/core-install.js` (edit) — Add durable manifest.degradations array so 'no silent caps' is auditable in ~/.gemini/.code-abyss-backup/manifest.json, not just console.
- `bin/lib/lifecycle/core-install.js` (edit) — gemini branch: explicit Stop-hook degradation log + durable manifest entry (mythos no-silent-caps). check_banned_openers backstop can't run on gemini; the character banned-openers rule survives prompt-only via GEMINI.md.
- `config/personas/_shared/kernel-routing.md` (create) — NEW shared fragment: the mythos kernel judgment router. Assembled into GEMINI.md (and every host context) by loadSharedBehavior, placed above skill-routing.md so the discipline spine + domain judges fire BEFORE exec-skill selection. This is the router-fusion layer.
- `bin/lib/style-registry.js` (edit) — Insert kernel-routing.md into the shared-behavior assembly order, immediately before skill-routing.md, so kernel judgment outranks exec routing in the rendered GEMINI.md. Shared/idempotent across all 4 hosts.
- `config/personas/_shared/skill-routing.md` (edit) — Fix STALE refs (building-ai-systems, coordinating-agents don't exist) and note kernel-routing precedence. Point AI/security lines through their kernel judges into real exec skill dirs.
- `config/personas/_shared/skill-routing.md` (edit) — Replace the two non-existent skill references with real directories.

**routing:**
- skills/_kernel/frontend (judge) → applying-ui-design-system, developing-mobile-apps  via kernel-routing.md in GEMINI.md; /frontend toml command reads the judge, then the judge routes to these exec skills
- skills/_kernel/backend (judge) → designing-architectures, developing-software, automating-devops  stack/logic/data/production judgment routes into existing gerund exec skills
- skills/_kernel/hardware (judge) → designing-hardware-products, operating-kicad-eda  part-selection/bench-debug judgment into the 2 hardware exec skills
- skills/_kernel/ml (judge) → building-agent-systems  method-selection ladder into the only agent-pipeline exec skill; replaces stale building-ai-systems ref
- skills/_kernel/security (judge) → securing-systems, defending-applications, architecting-security  threat-model/authorization-gate judgment into security exec skills

**conflicts:**
- Registration collision risk: the 9 kernel toml commands (/doctrine…/security) share the flat namespace ~/.gemini/commands/ with the 5 pre-existing invocable exec commands. VERIFIED no name clash — kernel bundle names (doctrine,methods,character,loop-engineering,frontend,backend,hardware,ml,security) are disjoint from exec skill names (gerund-form). collectSkills also runs validateUniqueSkillRecords (skill-registry.js:169) which would THROW on a duplicate, so a future name collision fails the install loudly rather than silently overwriting. Resolution: none needed now; keep the invariant that no exec skill is ever named after a kernel bundle.
- kernel-routing.md + SHARED_FILES_ORDER + skill-routing.md edits are CROSS-HOST (loadSharedBehavior feeds claude/codex/gemini identically via renderRuntimeGuidance). If the claude/codex sibling distribution tasks each emit the same 3 edits, they collide. Resolution: these 3 are L-KERNEL shared artifacts — apply ONCE. This gemini package lists them because gemini's router fusion is realized entirely through them (gemini has no bespoke router file of its own); they are idempotent (same bytes) so double-application is safe but should be deduped at merge time. openclaw does NOT get toml commands but DOES get the same fragment baked into AGENTS.md/SOUL.md via renderGeminiContext (core-install.js:415), so the fragment must stay host-agnostic — it is (only the one advisory note names Gemini, which is harmless on other hosts; if strict per-host text is wanted later, move that note out to a host-injected line).
- Stop-hook: unlike claude/codex, gemini's abyss-integration injectGeminiHooks (abyss-integration.js:94) only wires SessionStart+BeforeTool for abyss indexing and is --with-hooks/deprecated anyway — it is NOT a vehicle for the character Stop backstop, and gemini CLI exposes no Stop-equivalent event at all. Resolution: do not attempt to fake it via BeforeTool (wrong semantics — banned-openers is post-response). Degrade to prompt-only, logged via warn() + manifest.degradations (drafted above). The check_banned_openers.py file still ships to ~/.gemini/skills/_kernel/character/hooks/ (skill-glob copies it verbatim) so it is present-but-dormant, ready if gemini ever gains a Stop event.
- GEMINI.md is a single rendered blob (renderGeminiContext, no macro persona applied — personaSlug omitted at core-install.js:139), so the kernel-routing fragment inherits no {{self}}/{{user}} substitution. VERIFIED the fragment uses no macro tokens, so it renders byte-stable. If a future persona-scoped gemini render is added, kernel-routing must remain macro-free to preserve zero-drift.

**risks:**
- mid-tier enforcement gap is real and now documented, not hidden: on gemini the banned-openers rule and any other Stop-time invariant are advisory only. A weak model may still open with a capitulation phrase and nothing blocks it. This is the accepted cost of tier=mid; the manifest.degradations entry makes it auditable per host.
- Prompt-budget: GEMINI.md grows by the kernel-routing fragment (~30 lines) on every gemini install, and 9 new toml command files land in ~/.gemini/commands/. Small, but if the domain judges' SKILL.md bodies are large, `Read`-on-invoke cost per /judge command is higher than a native skill would be — acceptable since commands are lazy (only read when the slash-command fires).
- collectInvocableSkills depends on each kernel SKILL.md parsing valid frontmatter; a malformed kernel SKILL.md would make collectSkills THROW (skill-registry.js:150) and abort the WHOLE gemini install, not just skip that bundle. The sync-mythos.js frontmatter transform must guarantee valid frontmatter. Mitigation: gate the vendored kernel through the same verify-skills-contract.js check pre-publish.
- Router-fusion ordering assumes 'earlier in assembled context = higher priority' for the target models. That is a soft convention, not enforced. If a gemini-class model weights later text higher, kernel judgment could be overridden by the exec skill-routing block. Low risk (kernel-routing explicitly states the precedence in prose), but worth a behavior-battery check if mid-tier compliance looks weak.

## Host distribution — openclaw (tier=low): static AGENTS.md/SOUL.md bake of the mythos kernel router + 9 SKILL.md summaries (not full bodies), plus explicit Stop-hook degradation log.

openclaw is the lowest-capability host: no router, no slash-command generation, no skill invocation, no hooks. Its only runtime surface is two static files (`~/.openclaw/workspace/{AGENTS.md,SOUL.md}`), both written identically from `renderGeminiContext(...)` in core-install.js:415-418. The vendored kernel still ships to disk for free — manifest.json:28-32 globs `src:"skills"` into root `.openclaw` (target-registry.js:26,35), so `skills/_kernel/<bundle>/SKILL.md` lands at `~/.openclaw/skills/_kernel/<bundle>/SKILL.md` — but with no router/invocation those files are unreachable unless the static AGENTS.md points at them. So distribution to this host = a build-time bake: append a self-contained KERNEL block to the openclaw content containing (a) the mythos router (spine doctrine/methods/character/loop-engineering always-on, judges frontend/backend/hardware/ml/security routing into exec skills), (b) the 9 bundles' frontmatter name+description ONLY (read from the vendored SKILL.md at PKG_ROOT — NOT the full rule bodies), each with a Read: hint to its on-disk copy, and (c) an explicit degradation notice. Since openclaw has no Stop hook, the ported check_banned_openers.py backstop (mythos/character/hooks/check_banned_openers.py) cannot run here; per mythos "no silent caps" the install emits a warn() line stating banned-openers enforcement drops to advisory. New helper bin/lib/kernel-bake.js does the frontmatter extraction and rendering; core-install.js openclaw branch (lines 413-423) calls it. Zero manifest/adapter/command-gen change; the bake is fully owned by this slice and does not depend on the L-KERNEL shared-engine replacement landing first.</parameter>
<parameter name="verdict">distribute — bake router + 9 read-only SKILL.md summaries into openclaw's static AGENTS.md/SOUL.md (the only surface a no-router/no-hook host has), reading summaries from the already-vendored skills/_kernel/ at install time; log the Stop-hook degradation explicitly.

**files:**
- `/home/telagod/project/code-abyss/bin/lib/kernel-bake.js` (create) — New helper: extract name+description frontmatter from each vendored skills/_kernel/<bundle>/SKILL.md and render the static KERNEL bake block (router + 9 summaries + degradation notice) for capability-degraded hosts. Reads from PKG_ROOT so it works against the npm tarball's in-tree kernel; never emits full rule bodies.
- `/home/telagod/project/code-abyss/bin/lib/lifecycle/core-install.js` (edit) — Require the new kernel-bake helper at module top (sibling of gstack/abyss-integration requires).
- `/home/telagod/project/code-abyss/bin/lib/lifecycle/core-install.js` (edit) — openclaw branch: bake the mythos kernel (router + 9 read-only SKILL.md summaries) onto the static AGENTS.md/SOUL.md content, and emit the explicit Stop-hook / banned-openers degradation log per mythos 'no silent caps'.

**routing:**
- security (judge) → skills/securing-systems/  baked router line sends security/攻防-前置门 verdicts to the existing exec skill; openclaw agent Reads the on-disk exec SKILL.md path since it cannot invoke.
- backend (judge) → skills/designing-architectures/ + skills/developing-software/ + skills/automating-devops/  server-side/API/data/prod judgment fans into three exec skills depending on sub-task.
- frontend (judge) → skills/developing-software/  no dedicated UI exec skill exists in code-abyss's 30; frontend judgment lands on general dev exec. Gap to note for L-EXEC.
- ml (judge) → (none — building-ai-systems is the STALE nonexistent route)  ML/LLM judgment has NO exec-skill landing today; skill-routing.md:7 points at a skill that doesn't exist. L-EXEC gap — either add an ai/agents exec skill or route ml verdicts straight to execution with no exec hop.
- hardware (judge) → (none)  no hardware exec skill in code-abyss; hardware judgment is terminal advice on this product. Acceptable — flag in docs.
- doctrine (spine) → (no exec hop — governs delegation/done-gate directly)  spine bundles are always-on policy, not routed to exec; the bake marks them (脊柱) vs judges (裁决).

**conflicts:**
- Dead-on-disk kernel: manifest.json:28-32 copies skills/_kernel/ to ~/.openclaw/skills/_kernel/ (root .openclaw per target-registry.js:26,35), but openclaw has no router/command-gen and no skill-invocation surface, so those 9 SKILL.md files are unreachable by default. RESOLUTION: the bake is exactly what makes them reachable — each summary carries a `全文: ~/.openclaw/skills/_kernel/<bundle>/SKILL.md` Read-hint, so the on-disk copies serve as the full-body source the agent Reads on demand. Intended, not a defect; no manifest change needed.
- Stale routing bakes in: config/personas/_shared/skill-routing.md:7,9 route to non-existent exec skills (building-ai-systems, coordinating-agents). openclaw's AGENTS.md embeds loadSharedBehavior() output (style-registry.js:30-39 → renderRuntimeGuidance:320), so these dead routes ship into openclaw too. RESOLUTION: this file is owned by the L-KERNEL shared-engine slice; do NOT double-fix here. My bake appends the CORRECTED mythos router AFTER the shared block, so on reading order the kernel router is the authoritative last word; but the stale lines still ship until that slice removes ml/multi-agent dead routes. Flagged for L-KERNEL.
- Router duplication risk: if the L-KERNEL slice adds a shared `kernel-router.md` fragment into SHARED_FILES_ORDER (style-registry.js:19-28) above skill-routing.md, openclaw would then carry the router twice — once from the shared block, once from renderKernelBake's `### 路由` subsection. RESOLUTION: keep the bake self-contained for now (openclaw must not depend on the shared replacement landing first). When shared kernel-router lands, drop only the `### 路由` subsection from kernel-bake.js and keep summaries + degradation notice. See routingLinks note.
- No Stop hook on openclaw: bin/adapters/openclaw.js has no injector and openclaw.js:74-87 postOpenClaw does zero hook wiring (confirmed: 'OpenClaw 无额外推荐配置'). The ported check_banned_openers.py Stop backstop (mythos/character/hooks/check_banned_openers.py) therefore cannot run. RESOLUTION: per mythos 'no silent caps', the openclaw branch emits an explicit warn() that banned-openers enforcement is advisory-only here; the prose ban still bakes in via character bundle summary. (Claude/codex get the real Stop injector — owned by their host-distribution slices, not this one.)

**risks:**
- renderKernelBake reads skills/_kernel/*/SKILL.md from PKG_ROOT at install time; if `npm pack` shipped without running scripts/sync-mythos.js the kernel dir is absent and the bake degrades to fewer/zero summaries. Mitigated: per-bundle try/catch skips missing files and the branch warns when kernelCount<9. P0 scaffold already VERIFIED sync runs clean and vendors committed in-tree files, so the tarball ships them — but pre-pack sync must remain a release gate.
- Frontmatter description regex assumes a single-line or folded `description:` terminated by the next `\nKEY:` or block end. All 9 mythos SKILL.md use a single-line description (verified), so extraction is exact; a future multi-block description with embedded `word:` could truncate. Low risk, cosmetic (summary only).
- AGENTS.md and SOUL.md receive identical baked content (both written from unifiedContent, matching current behavior at core-install.js:417-418). If OpenClaw later diverges the semantics of the two files, the kernel block would need role-specific placement. No change to current parity.
- The bake grows openclaw's static context by ~9 description lines + router (~30 lines). Negligible vs full rule bodies (which are deliberately NOT baked — that was the tier=low decision), but it does add fixed prompt overhead to every openclaw session.

## codex host distribution

test

**files:**
- `/home/telagod/project/code-abyss/bin/adapters/codex.js` (edit) — test

**risks:**
- test risk